/* Write a function stringToNumber that takes a string input and tries to convert it to a number. After Conversion return it's type. If the conversion  fails, return "Not a number". */







/* Write a function whatAmI that takes an input and returns a string describing its type. If it's a number, return "I'm a number!", if it's a string, return "I'm a string!" */







/* Write a function isItTruthy that takes an input and returns "It's truthy!" if the value is truthy in JavaScript, or "It's falsey!" if it's value is falsey. */









/* From the given array mixedArr = ["hello",'hola',63,'merci',96,25,'ciao'], iterate and print only those elements which are of type number, otherwise skip the elements from the iteration  */









/* Write a function printMultiplicationTable(n) that returns the multiplication table for n. Values return in the array must be of the format 2 * 2 = 4 */











/* Write a function squareNumbers(arr) that returns square of numbers from an exisiting array- 
existingArr = [2,4,6,8] */







/* Employee Payroll & Bonus Finalizer

- Create an array of at least 4 employee objects, each with properties: name, baseSalary, performanceRating (a number from 1–5) and a method called getBonus that uses this to calculate a bonus based on the rating (employee will get bonus 2 X the base salary if his/her performance rating is greater than 2) 

- Write a separate function named calculateFinalPay that takes an employee object and an optional taxDeduction parameter with a default value, and returns the final payable amount (base salary + bonus − tax deduction).

- Write an arrow function named formatPayslip that takes a employee name, employee base salary and an final amount after calculation, and returns a readable string using template literals and a number method to format the amount nicely (e.g. with commas or fixed decimals).

- Using the appropriate HOF, on the employee array print formatted payslip strings for all employees.

- Using the appropriate HOF, get only the employees whose final pay exceeds the sum of 100000.

- Using the appropriate HOF, calculate the total payroll expense for the company based on the final amount given to each employee

*/